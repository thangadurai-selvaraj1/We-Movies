import 'package:flutter/material.dart';

import '../../core/index.dart';

class WeLogo extends StatelessWidget {
  const WeLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return CircleAvatar(
      child: Text(we),
    );
  }
}
