name: movie_app
description: "A new Movie project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Public Packages
  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.5
  geolocator: ^11.0.0
  geocoding: ^3.0.0
  http: ^1.2.0
  get_it: ^7.6.7
  internet_connection_checker_plus: ^2.2.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg